---

- hosts: localhost
  connection: local
  remote_user: root
  become: yes
  roles:
    - {role: jdauphant.nginx, tags: nginx}
  tasks:
   - file: dest=/opt/fooooo state=touch
   - command: id
     #become: true
